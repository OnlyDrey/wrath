<Page
    x:Class="Wrath.App.Pages.SessionHistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="16">
        <ListView x:Name="HistoryList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <TextBlock Text="{Binding Timestamp}" Width="220" />
                        <TextBlock Text="{Binding Protocol}" Width="80" />
                        <TextBlock Text="{Binding Host}" Width="180" />
                        <TextBlock Text="{Binding Succeeded}" Width="80" />
                        <TextBlock Text="{Binding Message}" Width="220" />
                        <TextBlock Text="{Binding ErrorMessage}" Width="260" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
